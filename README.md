Code is made by **Morten** and **ChatGPT**. 

This repository contains the code used for gnerating the results in Morten Svendgårds semester project
in the fall of 2024.

The code relates to the results in the following way:

- `test_through.py` generates all *track test* for the *SoftMin* and *Triangular* tests.
- `triangles_ablation.py` generates the *Triangular* ablation study.
- `input_constraints.py` generates the *Input Constraints* study.
- `input_constraints_counter_proof.py` generates the *Input Constraints Counter Proof* values.